 server {
       listen 80 default_server;
       listen [::]:80 default_server;
       
       server_name _;
       
       # Return 444 for requests that don't match any other server blocks
       return 444;
   }